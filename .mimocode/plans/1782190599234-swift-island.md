# Fix: REDO Edit Button + S.No/Date Across PG & UG Dashboards

## File 1: `client/src/pages/PGDashboard.jsx`

### A. Add missing departments to `departmentKeyToRoute` (line 20-27)
```js
const departmentKeyToRoute = {
  pedodontics: '/pedodontics',
  complete_denture: '/complete_denture',
  fpd: '/Fpd',
  implant: '/Implant',
  implant_patient: '/ImplantPatient',
  partial_denture: '/partial_denture',
  oral: '/casePortal?dept=oral',
  general: '/general-case-sheet',
  periodontics: '/casePortal?dept=periodontics',
  conservative: '/casePortal',
  conservativedentistry: '/casePortal',
  endodontics: '/casePortal',
};
```

### B. Add fallback in `startRedoEditFlow()` (line 1221)
```js
const routePath = departmentKeyToRoute[departmentKey] || getCaseRouteForDepartment(row?.department || departmentKey);
```

### C. Add S.No + Date columns to assigned-cases table

**Headers** (lines 2727-2734) — add `S.No` and `Date` as first two `<th>`:
```jsx
<tr>
  <th>S.No</th>
  <th>Date</th>
  <th>Patient Name</th>
  ...existing headers...
</tr>
```

**Row cells** — after `canViewCaseSheet` variable (~line 2750), add:
```js
const createdAt = row?.createdAt ? new Date(row.createdAt) : null;
```
Insert as first two `<td>` in each row:
```jsx
<td style={{ textAlign: 'center', whiteSpace: 'nowrap' }}>{index + 1}</td>
<td style={{ whiteSpace: 'nowrap' }}>{createdAt ? formatDate(createdAt) : '—'}</td>
```

---

## File 2: `client/src/pages/UGDashboard.jsx`

### A. Add missing departments to `departmentKeyToRoute` (line 20-31)
Add `general` and `periodontics`:
```js
general: '/oral-medicine',
periodontics: '/casePortal?dept=periodontics',
```

### B. Add fallback in `startRedoEditFlow()` (line 1235)
```js
const routePath = departmentKeyToRoute[departmentKey] || getCaseRouteForDepartment(row?.department || departmentKey);
```

### C. S.No + Date — already present ✓ (no change needed)

---

## Verification
1. Both dashboards → Assigned Cases table shows S.No (1,2,3…) and Date columns
2. REDO cases with any department (oral, general, periodontics, conservative, etc.) → Edit button enabled
3. Click Edit → navigates to correct department form with pre-filled data
